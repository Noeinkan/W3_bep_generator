---
description: Tailwind semantic tokens — dashed keys only, no camelCase
globs: **/*.css, tailwind.config.*, **/components/**/*.{js,jsx}
alwaysApply: false
---

# Tailwind Semantic Tokens

Use **dashed** keys/classes only (e.g. `text-ui-text-muted`, `bg-ui-primary-hover`). See **CLAUDE.md** for full rule.
