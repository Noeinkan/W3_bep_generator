---
description: Forms, state, API, styles, tests — where things live and how to add new ones
globs: src/**/*.{js,jsx}, server/**/*.js
alwaysApply: false
---

# Conventions

**Forms, state, API, styles, tests, DB:** See **CLAUDE.md** → Key conventions and Common patterns. Keep API/form changes in sync (routes + services + apiService; schema + component).
